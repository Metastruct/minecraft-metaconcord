subprojects {
	apply plugin: 'java'

	version = mod_version
	group = mod_group

	base {
		archivesName = "metaconcord-${project.name}"
	}

	java {
		toolchain {
			languageVersion = JavaLanguageVersion.of(21)
		}
	}
}
